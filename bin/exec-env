#!/usr/bin/env bash

set -euo pipefail

current_script_path=${BASH_SOURCE[0]}

echo "Hello: $ASDF_INSTALL_PATH"

# . $HOME/esp/v5.3/esp-idf/export.sh

# install_version "$ASDF_INSTALL_TYPE" "$ASDF_INSTALL_VERSION" "$ASDF_INSTALL_PATH"
